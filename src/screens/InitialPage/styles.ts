import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    centeredView: {
        flex: 1,
        alignItems: "center",
        backgroundColor: '#000',
    },

    logo: {
        width: 156,
        height: 156,
        marginTop: 50,
        marginBottom: 30
    },

    description: {
        color: '#fff',/*  */
        fontWeight: 'bold',
        fontSize: 24,
        marginBottom: 80
    }
});